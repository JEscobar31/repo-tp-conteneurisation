apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tp02-mychart-nginx.fullname" . }}-test-connection"
  labels:
    {{- include "tp02-mychart-nginx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tp02-mychart-nginx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
